import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.ajithsuryathati.shifttracker",
  appName: "ShiftTracker",
  webDir: "out",
};

export default config;
